import { LayoutContainer } from "../../components/layout/container"
import styles from './styles.module.scss';

export const ParsingPage = () => {
    return (
        <div className={styles.parsingPage}>
            <LayoutContainer />
        </div>
    )
}