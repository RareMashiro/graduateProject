import { CustomizeContainer } from "../customize/container"
import { ParsingContainer } from "../parsing/container"

export const Layout = () => {
    return (
        <>
            <>Layout section</>
            <ParsingContainer />
            <CustomizeContainer />
        </>
    )
}